# 0.12.0 验证记录

日期：2026-09-24。基线：0.11.0 / a074e4a；本机 Windows、Pi 0.87.1。

## 结果

- `npm run check`：通过。
- 完整 `npm test`：123 项通过，0 失败，0 跳过。
- 最后缩减内部旧参数、添加长工具参数折叠后，最终代码的相关回归：23 项通过，0 失败。范围包括真实 RPC、会话续接、能力、提示词、容量和运行时。
- 真实 Pi RPC 使用本地可控模型：运行中工具边界接收补充；一次最终报告直接结束；进程释放；空闲消息不启动；原会话明确 resume；上下文与暂存信息保留；阻塞直接返回；模型失败/中断；保存失败清理；会话丢失拒绝续接。
- TUI：在真实 RPC 保存的会话上运行实际组件，验证只读查看、不启动进程、不修改会话、无 C/M/A 输入动作；另外验证窄终端、滚动、历史结果、长工具输出与参数展开。
- `npm pack --dry-run --json`：pi-agent-deck@0.12.0，58 个文件；确认包含新会话组件和两份 0.12.0 文档，不包含测试目录、依赖或环境文件。
- `git diff --check`：通过。

## 证据与边界

[tui-session.txt](tui-session.txt) 是真实 Pi RPC 测试会话经实际 TUI 组件渲染的记录，120 列、48 行终端配置；不是设计稿。没有接管用户正在运行的终端做人工按键验收。

可控模型没有调用付费服务。这些检查验证接口、消息、会话、状态与资源行为，不证明真实主模型每次都能正确拆分和验收业务任务。模型策略仍通过已有回归验证。

测试日志未打包，完整回归与最终相关回归摘要保存在本记录。运行中的用户 Pi 会话未主动重载或终止。
