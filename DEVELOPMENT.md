# Agent Deck 开发约定

## 当前目标

保持派任务、自动收结果、继续、停止四个动作直观。0.9.0 模型侧只注册 `Agent`（创建）、`SendMessage`（补充或继续）、`TaskStop`（停止）。完整参数与示例见 [README 的工具接口](README.md#claude-风格工具接口)，参数 Schema 与运行时校验统一维护在 `tool-contract.ts`。用户通过 `/agent-create 描述` 或自然语言创建可复用角色，使用 `/agent-config` 编辑。

## 模块

- `index.ts`：Pi 工具、命令、当前父会话刷新和消息交付。
- `tool-contract.ts`：三个工具的参数校验、角色别名、模型别名与可用性检查、标题及公开回执。
- `task-identity.ts`：按所属父会话解析任务 ID 或实例名称；创建锁内检查名称并完成任务初始化。
- `config.ts`、`configuration-ui.ts`：全局设置、命令入口和配置路由。
- `config-editor.ts`、`menu.ts`：字段编辑、工具勾选和可滚动选择菜单；保存校验并保留其他设置。
- `agent-creation.ts`：当前模型生成角色、有限格式修正、防覆盖保存、可取消的生成界面及创建摘要。
- `agent-authoring.md`：供主 Agent 在自然语言创建/修改角色时按需读取的文件规范。
- `agents.ts`：内置、个人、可信项目覆盖；模型继承、工具排除和有效配置校验。
- `runtime.ts`：创建、继续、排队、取消、恢复和父会话索引。
- `runner.mjs`：独立子进程、工具事件、结果保存和执行超时。
- `persistence.mjs`：原子文件写入、短跨进程锁、每轮不可变结果和租约释放。
- `capacity.ts`：兼容的任务所有权记录；不做全局或角色数量限制。
- `routing.ts`、`model-profiles.json`：结合实际模型能力、提供商和固定参数构造候选快照。
- `router.mjs`：TypeSafe Choice 请求、响应校验、超时/错误回退；Runner 与试选命令共用。
- `routing-ui.ts`：独立开关、可取消试选；不创建子任务。
- `admission.ts`：Git worktree 根目录协调；非 Git 父子目录重叠互斥。
- `delivery.ts`：自然语言结果和父会话持久化回执去重。
- `child-runtime.ts`：内部提问工具；旧任务结构化报告兼容。
- `child-providers.ts`：声明式扩展提供商快照与子进程注册；禁止复制函数或加载父扩展的工具、钩子。
- `ui.ts`：当前会话任务面板及有界日志读取。
- `presentation.ts`：统一状态、准确计数、中文宽度、摘要和面板边框。

## 关键行为

新任务正常结束且有最终文本才算执行完成。用户界面显示“已返回结果”，主 Agent 负责判断实际完成情况。

`Agent` 只创建后台任务：短标题 `description`、完整说明 `prompt`、角色 `subagent_type`、实例名称 `name` 分别保存。公开 `agentId` 等于稳定的 runId，公开 `agentType` 才是角色 ID；内部历史字段 `RunDetails.agentId` 继续表示角色。工具 `content` 和 `details.publicResult` 同时提供身份与真实状态，内部记录放在 `details.run`。旧记录缺少新字段时兼容读取，不重写历史消息。

实例名称在同一父会话内不区分大小写，任务结束后仍绑定原任务。名称检查与初始化处于同一创建锁，已有任务记录是绑定依据；记录损坏时明确报错，不能当作名称空闲。补充和停止先检查父会话归属，再操作任务。

`SendMessage` 的正文按纯文本处理，摘要只用于预览和记录。选配、排队和运行中的消息持久化排队，在当前轮结束后按顺序处理；任务刚结束时收到的新消息也不能越过旧排队消息。继续保留子 Session 与符合当前模型策略的实际配置。`TaskStop` 清除排队消息，已经结束的任务保留原终态；无法确认进程退出时保留“停止未确认”。

补充消息先获得持久 ID，再准备恢复事务。`request.followUpBatch` 保存消息 ID、轮次 ID 与恢复状态，`status.followUpBatchId` 是提交凭据。请求先于状态写入；中断后在任务控制锁内补齐状态，排队状态可被正常调度。读取队列时过滤已提交批次；保存下一批前先移除上一批已消费项，不能先覆盖凭据。删除队列只是清理，失败不能重放消息。停止时先恢复已准备的状态再取消，避免准备中的消息在重启后复活。

创建 Session 前检查全部候选提供商。仅传递可序列化的声明式配置，私有快照单独保存到个人 `agent-deck/providers/`；不把密钥、请求头写进任务请求、结果或日志。子进程仍使用 `--no-extensions`，只由 child runtime 注册快照中的提供商。函数、OAuth 回调和原生 Provider 暂不支持，必须在创建前明确拒绝。提供商快照、用户认证文件均不能进入发布包。

父索引首次迁移时，跨会话扫描采用容错读取；确定属于当前父会话的索引记录仍严格读取，不能把损坏的名称绑定当成空闲。

每轮终态先独立保存到 `results/`，再更新当前状态。继续前也补存旧结果。不能因为调用了 sendMessage，就删除结果或永久标记送达。即使父 Agent 忙、结果仅在内存队列、已经自动继续下一轮，重启也要补送上一轮。

主 Agent 决定数量、角色、依赖和验收。Jev 只为新任务选择模型与思考强度，不修改角色配置。Runner 在后台选配；先记录 PID，再进行远程请求。启动与取消共享 spawn.lock；持久取消标记在显式继续时清除。已选参数原子写回 request.json，继续时不重新选配。

模型策略与组合集中于 `model-profiles.json`。审查（id=reviewer 或 reportProfile=审查）仅允许 GPT-5.6 Sol / xhigh、max；非审查禁止 GPT-5.6 Sol；GPT-6 Sol/Luna 最低 high；Astra 保留 low 到 max。共 13 个组合，分为审查 2 个、非审查 11 个。关闭 Jev、显式覆盖、模型别名、失败回退均不能绕过策略。继承值可调整到同一提供商的合规组合；显式冲突派遣前报错。候选还必须经过模型支持档位和有效映射筛选。Sol/Luna 的 Chat Completions 工具档位 off 低于策略下限，因此无有效候选。组合用途描述属于启发式策略，不能写成实测排名。

RunnerRequest.review 保存创建时的审查标记；删除 routing 后继续仍使用该快照。runtime 与独立 runner 在进程启动边界执行 router.mjs 的同一校验。旧排队任务违反策略则持久化失败；旧终态继续在修改请求和消息队列前拒绝。自动补充若因策略失败，设置 policyBlocked 防止无限重试，保留旧结果和待处理文字。旧记录没有审查标记时仅能可靠识别内置 reviewer；自定义审查须补标记后新建任务。已运行进程不自动换模或终止。

全局设置默认值由 `config.ts` 管理。`modelAliases` 默认空对象，只接受明确的 provider/model 映射。`Agent.model` 覆盖角色模型，Jev 选择尚未固定的部分；强度可在角色配置中固定。显式模型或有效角色模型不可用时，在创建 Session 和任务之前报错。旧 maxConcurrent 仅兼容读取，不参与调度。角色配置省略时限时继承全局默认，0 明确表示不限时。新任务保存角色配置；继续旧任务不重新套用角色修改，但必须通过当前强制模型策略。

工具别名只映射确实支持的 Pi 工具。未知字段和错误配置必须在派遣前报告，不能无声回退到权限更宽的内置角色。不会承诺 Claude Code 字段完全兼容。

创建命令只用本次角色描述生成配置，验证结果后保存到个人目录。模型输出不提供文件路径；角色重名自动加后缀。完整临时文件通过排他硬链接发布，避免并发覆盖或半写入文件被发现。取消和模型失败不创建角色；生成不自动派遣。常规缺省项继承，保留用户描述的约束。模型无法提供的能力作为限制持久化，不能只显示一次后丢失。

开关关闭时停用 `Agent`、`SendMessage` 和队列调度，保留 `TaskStop`、管理命令和结果交付。无参数 /agent-deck 直接切换。

界面按真实状态区分选配、运行、排队和等答复。选配未结束时显示模型待确定；选定后显示实际支持的模型与强度，结果快照保留选配原因和耗时。任务列表为状态与耗时预留宽度；按终端高度决定可见行数，内容少时收紧。日志默认跟随最新，上翻后按事件与换行位置保留锚点；End 恢复跟随。最多读取最近 128 KiB，不因重绘反复读取和解析整个文件。结果 Markdown 和日志换行缓存需在宽度或主题变化时失效。

配置界面编辑草稿，保存时才落盘。全局配置仅合并本次修改的字段；角色配置只在原文件仍一致时原子替换。修改其他字段保留权限，明确编辑工具时以勾选的有效列表为准。菜单有高度上限和滚动能力，不展开全部模型或角色。

## 验证

运行 npm run check 和 npm test。检查必须包含纯 JavaScript Runner 和 Router 语法。真实 Jev 质量评估需单独配置密钥，模拟接口测试不能证明选模效果。测试强制隔离 Agent 目录，使用假模型输出和受控 fixture 进程。

测试文件默认逐个运行，避免多个 Pi SDK 实例同时初始化造成机器提交内存不足；跨进程并发测试本身仍启动多个独立进程验证无数量上限或争抢写入锁。

重点覆盖：三个工具的实际调用和无副作用参数拒绝、标题/角色/实例身份、重名并发与损坏记录、消息顺序与旧队列兼容、模型别名与认证可用性、审查模型专用与强度下限、关闭选配及回退的合规性、旧任务和实际启动参数校验、旧数量限制不生效、TypeSafe HTTP 契约、候选可用性、取消选配、重复启动、继续不重选、工作区重叠、零时限、多轮结果恢复、跨会话隔离、配置保留、真实 Pi 加载器、中文长文本和缩放、日志跟随/暂停、菜单按键及外部修改冲突。不要通过源码字符串断言替代行为测试。

`node --import ./test/environment.mjs --import tsx scripts/render-tui-preview.ts` 从实际组件生成预览，强制使用独立测试目录；不请求模型、不启动任务。预览必须标明模拟数据，不当作原生终端截图或人工终端验收。

## 保持范围

不提供子会话跳转、不注册全局快捷键、不加入 DAG、自动合并、独立验收引擎或默认额外角色。旧 docs 审查和路线图仅供历史参考。

修改源码后需要 /reload；角色文件修改在新任务创建时读取。生产文件替换前核对备份清单，保留用户的外部修改。

变更工具契约时，同步核对 README、当前开发约定、角色编写指引和当前版本发布说明，并重新打包检查包内文档。旧版本发布说明、审查记录和原始开发计划保留为历史资料。分别记录本地部署、Git 提交、远端推送和 Release 状态，只有获得对应操作的证据后才报告完成。
